# Материалы для отчёта — ЛР №1, вариант 1

## Цель работы

Изучить работу последовательного интерфейса RS-232/COM-порта и реализовать
графическое приложение для обмена текстовыми данными между двумя станциями.

В варианте 1 выбирается скорость передачи. Длина
данных 8 бит, чётность None и 1 стоп-бит фиксированы. Каждый символ передаётся сразу
после ввода; Enter передаётся как перевод строки.

## Алгоритм

1. Программа показывает найденные COM-порты и варианты скорости передачи.
2. После однократного выбора порта и скорости она открывает порт с
   параметрами выбранная скорость / 8 / N / 1 и блокирует элементы выбора.
3. Каждый введённый печатный символ сразу записывается в порт отдельной
   операцией; счётчик переданных символов обновляется в области состояния.
4. Фоновый поток считывает входящие байты, декодирует UTF-8 и добавляет текст
   в область Received без блокирования интерфейса.
5. При закрытии окна поток останавливается, а порт закрывается.

На обоих концах линии необходимо выбрать одинаковую скорость. После открытия
порта программы обмениваются служебными сообщениями HELLO/ACK с выбранным
значением. Соединение считается подтверждённым только после получения ACK на
собственный HELLO. Поэтому при несовпадении обе программы показывают ошибку:
явно, если кадр прочитан, либо по независимому четырёхсекундному тайм-ауту,
если неверная скорость исказила кадр. Служебные сообщения не попадают в поле
принятых данных.

## Параметры COM-порта

| Параметр | Значение |
|---|---|
| Baud rate | 110–115200 (выбирается) |
| Byte size | 8 (фиксировано) |
| Parity | None / N (фиксировано) |
| Stop bits | 1 (фиксировано) |
| Timeout | 0.05 с чтение, 1 с запись |

## Проверка

Запустите два экземпляра `main.py` на концах пары COM-портов. В обоих окнах
выберите одинаковое количество стоп-битов. Текст, набранный в Send now одного
экземпляра, должен появляться посимвольно в Received второго; Activity должен
увеличивать счётчик отправленных символов.
